/*
import { Navigate } from "react-router-dom"

export const PrivateRoute = ({user, estadoUsuario, children}) => {
    if(!user){
        console.log(user)
        return <Navigate to='/' />
    }
    console.log(user)
    return children
}
*/